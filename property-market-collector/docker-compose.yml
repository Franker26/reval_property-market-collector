services:
  property-market-collector:
    build: .
    restart: unless-stopped
    ports:
      - "8200:8200"
    environment:
      - SERVICE_TOKEN=${SERVICE_TOKEN:-}
    volumes:
      - ./data:/app/data:z
