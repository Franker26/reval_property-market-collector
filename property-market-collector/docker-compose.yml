services:
  property-market-collector:
    build: .
    restart: unless-stopped
    ports:
      - "8100:8100"
    environment:
      - SERVICE_TOKEN=${SERVICE_TOKEN:-}
    volumes:
      - ./data:/app/data:z
